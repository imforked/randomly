/** Bounds for room creation; kept in sync with API validation (Zod). */
export const ROOM_SIZE_MIN = 1;
export const ROOM_SIZE_MAX = 20;

export const OPTIONS_PER_GUEST_MIN = 1;
export const OPTIONS_PER_GUEST_MAX = 5;
